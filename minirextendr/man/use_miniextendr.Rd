% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create.R
\name{use_miniextendr}
\alias{use_miniextendr}
\title{Add miniextendr to an existing package}
\usage{
use_miniextendr(
  path = ".",
  template_type = c("auto", "rpkg", "monorepo"),
  rpkg_name = NULL,
  miniextendr_version = "main",
  local_path = NULL,
  claude_skills = TRUE,
  crate_name = NULL
)
}
\arguments{
\item{path}{Path to the R package root, or \code{"."} to use the current directory.}

\item{template_type}{Template type. One of \code{"auto"} (the default; detect
from directory structure based on whether Cargo.toml or DESCRIPTION exists),
\code{"rpkg"} for a standalone R package, or \code{"monorepo"} for a Rust workspace.}

\item{rpkg_name}{Name of the R package subdirectory for monorepo template
(default: "rpkg"). Only used when template_type is "monorepo".}

\item{miniextendr_version}{Version of miniextendr to vendor (default: "main").
For monorepo projects, vendoring is only needed for CRAN submission.}

\item{local_path}{Optional path to local miniextendr repository. If provided,
vendors from local path instead of downloading from GitHub. Useful for
development and testing before the package is published.}

\item{claude_skills}{Whether to install the bundled Claude Code skill set
via \code{\link[=use_claude_skills]{use_claude_skills()}} (default \code{TRUE}). The skills are agent-facing
documentation only — they are \code{.Rbuildignore}d and never affect the built
package.}

\item{crate_name}{Rust workspace package to expose to R. Defaults to the root
library package, or the sole library member of a virtual workspace. Required
for virtual workspaces with multiple libraries. Only used for monorepos.}
}
\value{
Invisibly returns TRUE
}
\description{
Sets up all miniextendr scaffolding. Automatically detects whether to use
the standalone R package template or the monorepo template based on context:
}
\details{
\itemize{
\item \strong{In an R package} (has DESCRIPTION): Adds Rust scaffolding to current directory
\item \strong{In a Rust crate} (has Cargo.toml): Creates \verb{rpkg/} subdirectory with R package
}

This is an all-in-one function that calls all the individual \verb{use_miniextendr_*()}
functions.

If the target package has no \code{NAMESPACE} (e.g. it was set up via
\code{usethis::use_description()} in an existing repo rather than
\code{usethis::create_package()}), a minimal roxygen2-managed \code{NAMESPACE}
containing \verb{useDynLib(<pkg>, .registration = TRUE)} is seeded so the first
build does not fail on the configure-time guard. A later
\code{devtools::document()} populates it from your roxygen comments.
}
