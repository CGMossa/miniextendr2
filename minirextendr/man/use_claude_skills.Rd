% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use-claude-skills.R
\name{use_claude_skills}
\alias{use_claude_skills}
\title{Install the miniextendr Claude Code skill set into a package}
\usage{
use_claude_skills(path = ".")
}
\arguments{
\item{path}{Path to the project root, or \code{"."} to use the current directory.
For monorepo layouts call this at the workspace root (where agents run),
not inside the R package subdirectory.}
}
\value{
Invisibly returns TRUE
}
\description{
Copies the bundled agent skills (\verb{.claude/skills/miniextendr-*}) into the
project so coding agents working in the package get progressive-loading
context about the miniextendr build pipeline, conversions, class systems,
data.frame interface, parallelism rules, debugging recipes, and the
release/vendoring story -- all framed for the scaffolded-package layout
(\code{src/rust/lib.rs}, \code{configure}, \verb{tools/}).
}
\details{
Re-running upgrades in place: each bundled skill directory is deleted and
rewritten (the same delete-target-first convention as \code{\link[=use_miniextendr]{use_miniextendr()}}
templates), so stale copies never linger. Skill directories you added
yourself under \verb{.claude/skills/} are left untouched.

When the project root is an R package (a \code{DESCRIPTION} is present),
\verb{.claude/} and \code{AGENTS.md} are added to \code{.Rbuildignore} so agent guidance
never ships in the tarball. An \code{AGENTS.md} stub pointing non-Claude agents at the skill set is
written only if the file does not already exist -- it is user-owned content.
}
