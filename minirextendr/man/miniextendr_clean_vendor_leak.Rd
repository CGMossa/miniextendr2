% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean-vendor-leak.R
\name{miniextendr_clean_vendor_leak}
\alias{miniextendr_clean_vendor_leak}
\title{Remove a leaked inst/vendor.tar.xz}
\usage{
miniextendr_clean_vendor_leak(path = ".")
}
\arguments{
\item{path}{Path to the R package root, or \code{"."} to use the current
directory.}
}
\value{
Invisibly returns \code{TRUE} if the file was removed, \code{FALSE} if it
was already absent.
}
\description{
\code{inst/vendor.tar.xz} is the single signal that flips \code{./configure} into
offline tarball mode. Once that file exists, every subsequent
\verb{R CMD INSTALL}, \code{devtools::install()}, or \code{devtools::document()} call
builds against the vendored snapshot rather than pulling live workspace
or network sources.
}
\details{
This is intentional during CRAN submission prep (run
\code{\link[=miniextendr_vendor]{miniextendr_vendor()}} first, then \verb{R CMD build}). It becomes a trap
when a prior \verb{R CMD build} or check run leaves the file behind in your
source tree.

Call this function after any unexpected tarball-mode install to restore
normal source-mode dev iteration. If \code{Cargo.toml} still contains paths into
\verb{vendor/} (for example after \verb{cargo revendor --freeze}), the affected
dependency and patch entries are reported with repair instructions. Restore
their original source paths from before freezing; those paths cannot be
reconstructed from a frozen manifest alone.
}
\seealso{
\code{\link[=miniextendr_vendor]{miniextendr_vendor()}} to create the tarball intentionally,
\code{\link[=miniextendr_doctor]{miniextendr_doctor()}} to detect this and other configuration issues.
}
