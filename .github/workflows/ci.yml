name: CI

on:
  pull_request:
    # `labeled` lets the on-demand round-trip e2e job (r-roundtrip-e2e) be
    # triggered by applying the `run-e2e` label to a PR. The default activity
    # types (opened/synchronize/reopened) cover everything else; adding
    # labeled/unlabeled is additive — no existing job keys on the label.
    types: [opened, synchronize, reopened, labeled, unlabeled]
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * 6'

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  # Only PRs cancel their superseded runs. Main-push runs queue instead:
  # with cancel-on-main, every merge cancelled the previous merge's run, so
  # on busy days the main-tip guard (heap-check, devel/oldrel legs) never
  # actually completed (observed 2026-07-10: all recent main runs cancelled).
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  # CARGO_INCREMENTAL=0 — sccache can't cache rustc outputs when Cargo's
  # incremental compilation embeds per-invocation unique hashes into crate
  # metadata. With incremental off, rustc outputs are deterministic and
  # sccache hit rates jump from ~50% to near-100% across workspace crates.
  # See CLAUDE.md "sccache and [profile.dev] incremental" for details.
  CARGO_INCREMENTAL: "0"

jobs:
  # ===========================================================================
  # Generated file hygiene check
  # ===========================================================================
  generated-files-check:
    name: Generated Files Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Check for committed generated files
        run: |
          # Files that should NOT be tracked in git (generated by configure
          # or by the host cdylib pass).
          # NOTE: Cargo.toml and stub.c ARE intentionally tracked — they enable
          # cargo check / R CMD build without running configure first.
          # wrappers.R + wasm_registry.rs are generated by the cdylib pass on every
          # `R CMD INSTALL`; untracked to avoid constant merge conflicts (they ship
          # in the tarball from disk, regenerated by `just r-cmd-build`).
          generated_files=(
            "rpkg/src/Makevars"
            "rpkg/src/rust/.cargo/config.toml"
            "rpkg/R/miniextendr-wrappers.R"
            "rpkg/src/rust/wasm_registry.rs"
          )

          # Directories that should NOT be tracked
          generated_dirs=(
            "rpkg/src/rust/target"
            "rpkg/src/rust/ra_target"
            "rpkg/src/rust/.cargo"
            "rpkg/vendor"
          )

          failed=0

          echo "Checking for generated files that should not be committed..."
          for f in "${generated_files[@]}"; do
            if git ls-files --error-unmatch "$f" &>/dev/null; then
              echo "::error::Generated file '$f' is tracked in git. Remove it with: git rm --cached $f"
              failed=1
            fi
          done

          echo "Checking for generated directories that should not be committed..."
          for d in "${generated_dirs[@]}"; do
            if git ls-files -- "$d" | grep -q .; then
              echo "::error::Generated directory '$d' is tracked in git. Remove it with: git rm -r --cached $d"
              failed=1
            fi
          done

          if [ "$failed" -eq 0 ]; then
            echo "No generated files found in git tracking."
          else
            echo ""
            echo "These files are generated by 'just configure' and should not be committed."
            echo "Add them to .gitignore if not already present."
            exit 1
          fi

  # ===========================================================================
  # Version consistency check
  # ===========================================================================
  version-check:
    name: Version Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install just
        uses: taiki-e/install-action@just

      - name: Check version consistency
        run: |
          # Extract version from Cargo.toml [workspace.package]
          CARGO_VERSION=$(sed -n '/\[workspace\.package\]/,/^\[/{ s/^version = "\(.*\)"/\1/p; }' Cargo.toml | head -1)

          # Extract version from rpkg/DESCRIPTION
          R_VERSION=$(grep '^Version:' rpkg/DESCRIPTION | sed 's/Version: //')

          echo "Cargo.toml version: $CARGO_VERSION"
          echo "DESCRIPTION version: $R_VERSION"

          # Compare (allow R dev versions like X.Y.Z.9000 to match X.Y.Z)
          CARGO_BASE=$(echo "$CARGO_VERSION" | grep -oE '^[0-9]+\.[0-9]+\.[0-9]+')
          R_BASE=$(echo "$R_VERSION" | grep -oE '^[0-9]+\.[0-9]+\.[0-9]+')

          if [ "$CARGO_BASE" != "$R_BASE" ]; then
            echo "::error::Version mismatch! Cargo.toml ($CARGO_VERSION) vs DESCRIPTION ($R_VERSION)"
            echo "Run: ./scripts/bump-version.sh $CARGO_VERSION"
            exit 1
          fi

          echo "Versions match!"

  # ===========================================================================
  # Change detection (path-based gating)
  # ===========================================================================
  changes:
    name: Detect changes
    runs-on: ubuntu-latest
    outputs:
      rust: ${{ steps.filter.outputs.rust }}
      r: ${{ steps.filter.outputs.r }}
      docs: ${{ steps.filter.outputs.docs }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Determine changed paths
        id: filter
        uses: dorny/paths-filter@v4
        with:
          filters: |
            rust:
              - '**/*.rs'
              - 'Cargo.toml'
              - 'Cargo.lock'
              - '**/Cargo.toml'
              - '**/Cargo.lock'
              - '.cargo/**'
              - '.github/workflows/**'
              - 'justfile'
            r:
              - 'rpkg/**'
              - 'minirextendr/**'
              - 'tests/cross-package/**'
              - 'miniextendr-api/**'
              - 'miniextendr-macros/**'
              - 'miniextendr-engine/**'
              - 'miniextendr-lint/**'
              - '**/*.R'
              - '**/*.r'
              - '**/configure.ac'
              - '**/configure'
              - '**/*.in'
              - 'Cargo.toml'
              - 'Cargo.lock'
              - '**/Cargo.toml'
              - '**/Cargo.lock'
              - '.cargo/**'
              - '.github/workflows/**'
              - 'justfile'
            docs:
              - 'rpkg/man/**'
              - '**/*.rs'
              - 'Cargo.toml'
              - 'Cargo.lock'
              - '**/Cargo.toml'
              - '**/Cargo.lock'
              - 'README*'
              - 'docs/**'
              - 'site/**'
              - 'scripts/docs-to-site.sh'

  # ===========================================================================
  # Docs link check (Zola internal anchors + external URLs, see
  # plans/2026-07-13-audit-doc-link-anchors.md). Fast, no R/Rust build —
  # gated on doc/site changes only so it also runs on docs-only PRs that
  # skip the rust/r filters below.
  # ===========================================================================
  docs-link-check:
    name: Docs Link Check
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.docs == 'true'
    steps:
      - uses: actions/checkout@v7

      - name: Install just
        uses: taiki-e/install-action@just

      - name: Install Zola
        run: |
          ZOLA_VERSION="0.22.1"
          curl -sL "https://github.com/getzola/zola/releases/download/v${ZOLA_VERSION}/zola-v${ZOLA_VERSION}-x86_64-unknown-linux-gnu.tar.gz" | tar xz
          sudo mv zola /usr/local/bin/

      - name: Check doc links (internal anchors + external URLs)
        run: just site-check

  # ===========================================================================
  # Sync checks (vendor, templates, lint parser)
  # ===========================================================================
  sync-checks:
    name: Sync Checks
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.rust == 'true' || needs.changes.outputs.r == 'true'
    env:
      # wrappers-sync-check does a full source-mode `R CMD INSTALL` (Rust
      # compile) — without sccache it was the job's dominant cost (~13 min
      # cold vs ~5 min warm in the identically-shaped r-tests job).
      SCCACHE_GHA_ENABLED: "true"
      RUSTC_WRAPPER: "sccache"
    steps:
      - uses: actions/checkout@v7
        with:
          lfs: true

      - name: Install just
        uses: taiki-e/install-action@just

      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: release
          use-public-rspm: true

      - name: Install autoconf
        # ubuntu-latest images ship autoconf; the apt path is only a fallback
        # for image changes (saves ~30-60 s of apt-get update per job).
        run: |
          command -v autoconf >/dev/null || { sudo apt-get update && sudo apt-get install -y autoconf; }

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - uses: mozilla-actions/sccache-action@v0.0.10

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: rpkg/src/rust -> target
          shared-key: sync-checks
          cache-on-failure: true

      - name: Set up cargo-revendor (install only)
        uses: ./.github/actions/setup-vendor
        with:
          configure-and-vendor: 'false'

      - name: Install R packages (devtools, roxygen2)
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          working-directory: rpkg
          extra-packages: any::devtools, any::roxygen2
          needs: check

      - name: Configure (dev mode — no vendor tarball yet)
        run: just configure

      - name: Check R wrappers / NAMESPACE / man sync
        # MUST run in dev mode (no inst/vendor.tar.xz yet) — tarball mode
        # short-circuits wrapper generation and expects pre-shipped files.
        run: just wrappers-sync-check

      - name: Vendor
        run: just vendor

      - name: Check vendor sync
        run: just vendor-sync-check

      - name: Check templates sync
        run: just templates-check

      - name: AGENTS.md sibling check
        run: just agents-md-check

      - name: Check docs index coverage
        shell: bash
        run: |
          set -euo pipefail
          missing=0
          for doc in docs/*.md; do
            name=${doc##*/}
            [ "$name" = README.md ] && continue
            # Match a real Markdown link target — `](name)` or `](name#anchor)` —
            # so a filename that is merely a substring of another doc's link
            # (e.g. COERCE.md inside R_COERCE.md) does not count as an entry.
            if ! grep -Fq "]($name)" docs/README.md && ! grep -Fq "]($name#" docs/README.md; then
              echo "::error file=docs/README.md::missing index entry for $name"
              missing=1
            fi
          done
          exit "$missing"

      - name: Check end-user Claude skills vs scaffold layout
        run: just user-skills-check

      - name: Check lint parser sync
        run: just lint-sync-check

  # ===========================================================================
  # Rust lint (rustfmt + clippy) — runs independently of R CMD check
  # ===========================================================================
  rust-lint:
    name: Rust lint
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    env:
      SCCACHE_GHA_ENABLED: "true"
      RUSTC_WRAPPER: "sccache"

    steps:
      - uses: actions/checkout@v7

      # miniextendr-api/build.rs calls `R RHOME` (or reads $R_HOME) to resolve
      # libR's location, which only matters at link time. Lint steps (fmt +
      # clippy) never link, so pointing R_HOME at an existing dummy directory
      # satisfies build.rs's existence check without installing R. Saves ~1 min
      # of setup-r time on every lint run.
      - name: Set dummy R_HOME
        run: echo "R_HOME=$RUNNER_TEMP" >> "$GITHUB_ENV"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - uses: mozilla-actions/sccache-action@v0.0.10

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: rust-lint
          cache-on-failure: true

      # `just fmt-check` iterates every manifest (root workspace +
      # rpkg/src/rust + tests/cross-package/*), not just the root workspace —
      # the non-root manifests are separate cargo workspaces that `cargo fmt
      # --all` alone never reaches. `cargo fmt --check` needs no dep
      # resolution / .cargo/config.toml, so this runs without `just configure`.
      - name: Install just
        uses: taiki-e/install-action@just

      # Committed-lock shape gate (#1222): the committed rpkg/src/rust/Cargo.lock
      # must stay in tarball-shape (framework crates carry their canonical
      # `source = "git+…"` lines — see docs/CARGO_LOCK_SHAPE.md). Local dev
      # recipes auto-restore drift (`just cargo-lock-restore`), but nothing
      # gated the *committed* file, and it silently drifted to path-shape on
      # main for 20+ commits. Pure grep, no toolchain needed — runs first so
      # a drifted lock fails fast.
      - name: Lock shape (committed Cargo.lock)
        id: lock_shape
        continue-on-error: true
        run: just lock-shape-check 2>&1 | tee "$RUNNER_TEMP/lock_shape.log"
        shell: bash

      - name: Rustfmt
        id: rustfmt
        continue-on-error: true
        run: just fmt-check 2>&1 | tee "$RUNNER_TEMP/rustfmt.log"
        shell: bash

      - name: Clippy (default features)
        id: clippy_default
        continue-on-error: true
        run: cargo clippy --workspace --all-targets --locked -- -D warnings 2>&1 | tee "$RUNNER_TEMP/clippy_default.log"
        shell: bash

      - name: Clippy (all features)
        id: clippy_all
        continue-on-error: true
        # The codegen-selector half is derived from miniextendr-api's
        # `full-codegen` aggregate (#677) so it can't drift from the Cargo.toml
        # definition — adding a new `*-default` / nonapi / connections selector
        # there updates CI automatically. `full-codegen` picks the `r6-default`
        # side of the r6/s7 mutex; the `s7-default` side gets its own step
        # below (nothing else compiles it — the `--workspace` steps never
        # reach rpkg/src/rust, a standalone workspace, and rpkg's
        # tools/detect-features.R denylists `s7-default` anyway). We still
        # can't use `--features full` directly: it adds heavy deps (arrow,
        # datafusion, log) we don't need here. So we pass `full-codegen` plus
        # a curated integration list (the lighter crates) — the latter is the
        # only part still hand-maintained, and CI signal matches what rpkg
        # actually enables.
        run: >-
          cargo clippy --workspace --all-targets --locked
          --features full-codegen,rayon,rand,rand_distr,either,ndarray,nalgebra,serde,serde_json,num-bigint,rust_decimal,ordered-float,uuid,regex,indexmap,time,num-traits,bytes,num-complex,url,sha2,bitflags,bitvec,aho-corasick,toml,tabled,raw_conversions,vctrs,tinyvec,borsh,jiff,blake3,md5,globset,zstd
          -- -D warnings 2>&1 | tee "$RUNNER_TEMP/clippy_all.log"
        shell: bash

      - name: Clippy (all features, s7-default)
        id: clippy_all_s7
        continue-on-error: true
        # Same integration list as clippy_all with the r6/s7 mutex flipped via
        # the `full-codegen-s7` aggregate — the only compile gate anywhere for
        # the s7 side of the mutex. Keep the integration list identical to the
        # step above; only the aggregate differs.
        run: >-
          cargo clippy --workspace --all-targets --locked
          --features full-codegen-s7,rayon,rand,rand_distr,either,ndarray,nalgebra,serde,serde_json,num-bigint,rust_decimal,ordered-float,uuid,regex,indexmap,time,num-traits,bytes,num-complex,url,sha2,bitflags,bitvec,aho-corasick,toml,tabled,raw_conversions,vctrs,tinyvec,borsh,jiff,blake3,md5,globset,zstd
          -- -D warnings 2>&1 | tee "$RUNNER_TEMP/clippy_all_s7.log"
        shell: bash

      # cargo-revendor is a standalone workspace excluded from the root
      # workspace, so the `--workspace` clippy steps above never reach it
      # (#778). It has no R linkage, so it lints without configure/R setup.
      - name: Clippy (cargo-revendor)
        id: clippy_revendor
        continue-on-error: true
        run: cargo clippy --manifest-path cargo-revendor/Cargo.toml --all-targets --locked -- -D warnings 2>&1 | tee "$RUNNER_TEMP/clippy_revendor.log"
        shell: bash

      - name: Rustdoc (cargo-revendor)
        id: doc_revendor
        continue-on-error: true
        env:
          RUSTDOCFLAGS: "-D warnings"
        run: cargo doc --no-deps --document-private-items --manifest-path cargo-revendor/Cargo.toml --locked 2>&1 | tee "$RUNNER_TEMP/doc_revendor.log"
        shell: bash

      - name: Fail if any Rust check failed
        if: always()
        shell: bash
        run: |
          set -uo pipefail
          failed=0

          lock_shape="${{ steps.lock_shape.outcome }}"
          rustfmt="${{ steps.rustfmt.outcome }}"
          clippy_default="${{ steps.clippy_default.outcome }}"
          clippy_all="${{ steps.clippy_all.outcome }}"
          clippy_all_s7="${{ steps.clippy_all_s7.outcome }}"
          clippy_revendor="${{ steps.clippy_revendor.outcome }}"
          doc_revendor="${{ steps.doc_revendor.outcome }}"

          echo "lock_shape:       $lock_shape"
          echo "rustfmt:          $rustfmt"
          echo "clippy_default:   $clippy_default"
          echo "clippy_all:       $clippy_all"
          echo "clippy_all_s7:    $clippy_all_s7"
          echo "clippy_revendor:  $clippy_revendor"
          echo "doc_revendor:     $doc_revendor"
          echo

          dump_failed() {
            local name="$1" outcome="$2" log="$RUNNER_TEMP/$1.log"
            if [[ "$outcome" != "success" ]]; then
              echo "::group::$name failure output"
              if [[ -f "$log" ]]; then
                cat "$log"
              else
                echo "(no log captured for $name)"
              fi
              echo "::endgroup::"
              failed=1
            fi
          }

          dump_failed lock_shape       "$lock_shape"
          dump_failed rustfmt          "$rustfmt"
          dump_failed clippy_default   "$clippy_default"
          dump_failed clippy_all       "$clippy_all"
          dump_failed clippy_all_s7    "$clippy_all_s7"
          dump_failed clippy_revendor  "$clippy_revendor"
          dump_failed doc_revendor     "$doc_revendor"

          if [[ "$failed" -ne 0 ]]; then
            echo "::error::One or more Rust checks failed (output dumped above)"
            exit 1
          fi

  # ===========================================================================
  # Rust tests (cargo test) — runs independently of R CMD check
  # ===========================================================================
  rust-tests:
    name: Rust tests
    runs-on: ubuntu-latest
    timeout-minutes: 45
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    env:
      SCCACHE_GHA_ENABLED: "true"
      RUSTC_WRAPPER: "sccache"

    steps:
      - uses: actions/checkout@v7
        with:
          lfs: true

      # Unlike rust-lint's dummy R_HOME, tests need a real R: miniextendr-api's
      # integration tests link against libR and boot an embedded R at runtime
      # (miniextendr-engine resolves R_HOME via `R RHOME` on PATH).
      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: release
          use-public-rspm: true

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - uses: mozilla-actions/sccache-action@v0.0.10

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: rust-tests
          cache-on-failure: true

      - name: Put libR on LD_LIBRARY_PATH
        run: |
          echo "LD_LIBRARY_PATH=$(R RHOME)/lib" >> "$GITHUB_ENV"

      # --no-fail-fast matches `just test`: one failing binary shouldn't hide
      # failures in the suites behind it (each red run should show the full
      # failure surface, not the first casualty).
      - name: Tests (root workspace)
        run: cargo test --workspace --locked --no-fail-fast

      # The ui_vctrs trybuild suite is `#[cfg(feature = "vctrs")]` and `vctrs`
      # is not a default feature, so the feature-less --workspace run above
      # compiles it to an empty test binary and its `.stderr` snapshots drift
      # undetected (#1336). Run it explicitly; kept separate from the workspace
      # step so the vctrs feature never leaks into other suites' rendering.
      - name: Tests (ui_vctrs trybuild, vctrs feature)
        run: cargo test -p miniextendr-macros --test ui_vctrs --features vctrs --locked --no-fail-fast

      # The ndarray integration tests are feature-gated (`#[cfg(feature =
      # "ndarray")]`) and invisible to the default-features workspace run
      # above — without an explicit feature leg they never execute in CI
      # (the silent feature-gate mismatch; surfaced by #1348). Scoped to the
      # ndarray test binaries to keep the step cheap.
      - name: Tests (miniextendr-api, ndarray feature)
        run: cargo test -p miniextendr-api --locked --no-fail-fast --features ndarray --test ndarray_generic --test ndarray_all_types --test ndarray_string

      # cargo-revendor is a standalone workspace excluded from the root
      # workspace, so the `--workspace` step above never reaches it (#778).
      - name: Tests (cargo-revendor)
        run: cargo test --manifest-path cargo-revendor/Cargo.toml --locked --no-fail-fast

  # ===========================================================================
  # Feature-combination compile check (scheduled only)
  # ===========================================================================
  # Runs `just check-features` so the recipe can't silently rot when a feature
  # is added/removed (it once carried a deleted feature for months — audit A3).
  # Weekly-only: the final combo pulls full-integrations (arrow + datafusion),
  # which is too heavy for a per-PR gate.
  check-features:
    name: Feature combos
    runs-on: ubuntu-latest
    timeout-minutes: 60
    if: github.event_name == 'schedule'
    env:
      SCCACHE_GHA_ENABLED: "true"
      RUSTC_WRAPPER: "sccache"

    steps:
      - uses: actions/checkout@v7

      # Same trick as rust-lint: cargo check never links libR, so a dummy
      # R_HOME satisfies build.rs without installing R.
      - name: Set dummy R_HOME
        run: echo "R_HOME=$RUNNER_TEMP" >> "$GITHUB_ENV"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - uses: mozilla-actions/sccache-action@v0.0.10

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: check-features
          cache-on-failure: true

      - name: Install just
        uses: taiki-e/install-action@just

      - name: Check feature combinations
        run: just check-features

  # ===========================================================================
  # R Package: R CMD check on Linux
  # ===========================================================================
  r-check-linux:
    name: R CMD check / Linux ${{ matrix.r }}
    runs-on: ubuntu-latest
    timeout-minutes: 60
    continue-on-error: ${{ matrix.allow_failure || false }}
    needs: changes
    if: needs.changes.outputs.r == 'true'
    strategy:
      fail-fast: false
      # PRs check on R release only. devel (advisory — continue-on-error) and
      # oldrel-1 rarely catch anything release doesn't on a per-PR basis but
      # cost ~2x45 min of runners per push; they still run on push-to-main,
      # the weekly schedule, and workflow_dispatch, so a genuine devel/oldrel
      # breakage surfaces within one merge.
      matrix:
        include: ${{ github.event_name == 'pull_request'
          && fromJSON('[{"r":"release"}]')
          || fromJSON('[{"r":"release"},{"r":"devel","allow_failure":true},{"r":"oldrel-1"}]') }}

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes
      SCCACHE_GHA_ENABLED: "true"
      RUSTC_WRAPPER: "sccache"
      # The gctorture-heavy test files (~31 of the suite's ~34 min) run once
      # per PR in the dedicated r-stress-tests job — not in every check leg.
      # See rpkg/tests/testthat/helper-gc-stress.R.
      MINIEXTENDR_SKIP_STRESS: "1"

    steps:
      - uses: actions/checkout@v7
        with:
          lfs: true

      - name: Install just
        uses: taiki-e/install-action@just

      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ matrix.r }}
          use-public-rspm: true

      - name: Setup R dependencies
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          working-directory: rpkg
          extra-packages: any::rcmdcheck
          needs: check

      - name: Install autoconf
        # ubuntu-latest images ship autoconf; the apt path is only a fallback
        # for image changes (saves ~30-60 s of apt-get update per job).
        run: |
          command -v autoconf >/dev/null || { sudo apt-get update && sudo apt-get install -y autoconf; }

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - uses: mozilla-actions/sccache-action@v0.0.10

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: rpkg/src/rust -> target
          shared-key: r-linux-${{ matrix.r }}
          cache-on-failure: true

      - name: Configure Linux
        run: |
          echo "LD_LIBRARY_PATH=$(R RHOME)/lib" >> "$GITHUB_ENV"

      # NOTE: the vendor tarball is intentionally NOT cached. It is always
      # regenerated by `just vendor` (--force) in the step below, so caching it
      # bought nothing — and a restored tarball flipped `just configure` into
      # tarball mode, which made cargo-revendor drop in-PR workspace-crate edits
      # and re-fetch the framework from git@main (latch leak, #441 / #865).
      - name: Set up cargo-revendor and vendor
        uses: ./.github/actions/setup-vendor

      - name: R CMD check
        uses: r-lib/actions/check-r-package@v2
        with:
          working-directory: rpkg
          args: 'c("--no-manual", "--as-cran")'
          error-on: '"warning"'
          check-dir: '"check"'

      - name: Print failed tests
        if: always()
        run: |
          for f in rpkg/check/*.Rcheck/tests/*.Rout.fail; do
            [ -f "$f" ] && echo "=== $f ===" && tail -n100 "$f"
          done || true

      - name: Print 00check.log and 00install.out
        if: always()
        run: |
          echo "=== 00install.out ==="
          cat rpkg/check/*.Rcheck/00install.out 2>/dev/null || echo "00install.out not found"
          echo ""
          echo "=== 00check.log ==="
          cat rpkg/check/*.Rcheck/00check.log 2>/dev/null || echo "00check.log not found"

      - name: Upload check results
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: logs-linux-r${{ matrix.r }}
          path: |
            rpkg/check
            rpkg/config.log
          retention-days: 7

  # ===========================================================================
  # R Package: R CMD check on macOS
  # ===========================================================================
  # On-demand: this job runs only on workflow_dispatch + weekly cron (Sat 06:00 UTC).
  # Non-gating — intentionally not in ci-success.needs. Tracks #95 (macOS x86_64
  # SIGABRT regression probe). To force a run: gh workflow run ci.yml --ref main
  r-check-macos:
    name: R CMD check / macOS ${{ matrix.arch }}
    runs-on: ${{ matrix.runner }}
    if: github.event_name == 'workflow_dispatch' || github.event_name == 'schedule'
    timeout-minutes: 90
    needs: changes
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-15
            arch: arm64
          - runner: macos-15-intel
            arch: x86_64

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes
      SCCACHE_GHA_ENABLED: "true"

    steps:
      - uses: actions/checkout@v7
        with:
          lfs: true

      - name: Install just
        run: |
          brew install just
          echo "$(brew --prefix)/bin" >> $GITHUB_PATH

      - name: Install autoconf
        run: |
          brew install autoconf

      # Gotcha 5: CRAN's macOS binary R is built against a specific Xcode SDK
      # (Xcode 16.2 for x86_64 / Xcode 26.0 for arm64) and deployment target
      # (11.0 for x86_64 / 14.0 for arm64). Pin the SDK so our CI exercises
      # the same toolchain ABI we tell downstream maintainers to use.
      # Mirrors `minirextendr/inst/templates/r-release.yml`; docs are the
      # single source of truth, this duplication is by design.
      # See docs/RELEASE_WORKFLOW.md Gotcha 5.
      - name: Pin macOS SDK and deployment target
        run: |
          if [ "$(uname -m)" = "x86_64" ]; then
            sudo xcode-select -s /Applications/Xcode_16.2.app || true
            echo "MACOSX_DEPLOYMENT_TARGET=11.0" >> $GITHUB_ENV
          else
            sudo xcode-select -s /Applications/Xcode_26.0.app || true
            echo "MACOSX_DEPLOYMENT_TARGET=14.0" >> $GITHUB_ENV
          fi
          echo "xcode is set: $(xcode-select --print-path)"
          xcrun --show-sdk-version || true

      # Gotcha 6: CRAN's macOS binary R links against /opt/R/<arch>/lib system
      # libraries (libcurl, openssl, libtiff, ...) curated by
      # r-universe-org/macos-libs. Any Rust crate with a -sys C dep using
      # pkg-config will resolve against these — the same library set R itself
      # was built against. Pre-empts the "links fine locally, segfaults under
      # CRAN's R" trap. Inlined from r-devel/actions/setup-macos-tools@ec72e88.
      # See docs/RELEASE_WORKFLOW.md Gotcha 6.
      - name: Download CRAN system libraries
        run: |
          sudo mkdir -p /opt
          sudo chown $USER /opt
          curl --retry 3 --fail-with-body -sSL \
            https://github.com/r-universe-org/macos-libs/releases/download/2025-12-13/cranlibs-everything.tar.xz \
            -o libs.tar.xz
          sudo tar -xf libs.tar.xz -C / opt
          rm -f libs.tar.xz
          echo "PKG_CONFIG_PATH=/opt/R/$(uname -m)/lib/pkgconfig:/opt/R/$(uname -m)/share/pkgconfig" >> $GITHUB_ENV

      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: release
          use-public-rspm: true

      - name: Setup R dependencies
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          working-directory: rpkg
          extra-packages: any::rcmdcheck
          needs: check

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Setup sccache
        id: sccache
        uses: mozilla-actions/sccache-action@v0.0.10
        continue-on-error: true

      - name: Set RUSTC_WRAPPER if sccache available
        if: steps.sccache.outcome == 'success'
        run: echo "RUSTC_WRAPPER=sccache" >> $GITHUB_ENV

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: rpkg/src/rust -> target
          shared-key: r-macos-${{ matrix.arch }}
          cache-on-failure: true

      - name: Set up cargo-revendor and vendor
        uses: ./.github/actions/setup-vendor

      - name: R CMD check
        uses: r-lib/actions/check-r-package@v2
        with:
          working-directory: rpkg
          args: 'c("--no-manual", "--as-cran")'
          error-on: '"warning"'
          check-dir: '"check"'
          # No NOT_CRAN — configure.ac detects tarball install from inst/vendor.tar.xz

      - name: Print failed tests
        if: always()
        run: |
          for f in rpkg/check/*.Rcheck/tests/*.Rout.fail; do
            [ -f "$f" ] && echo "=== $f ===" && tail -n100 "$f"
          done || true

      - name: Print 00check.log and 00install.out
        if: always()
        run: |
          echo "=== 00install.out ==="
          cat rpkg/check/*.Rcheck/00install.out 2>/dev/null || echo "00install.out not found"
          echo ""
          echo "=== 00check.log ==="
          cat rpkg/check/*.Rcheck/00check.log 2>/dev/null || echo "00check.log not found"

      - name: Upload check results
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: logs-macos-${{ matrix.arch }}
          path: |
            rpkg/check
            rpkg/config.log
          retention-days: 7

  # ===========================================================================
  # R Package: R CMD check on Windows
  # ===========================================================================
  # DISABLED: Windows CI is currently disabled. Uncomment the block below to
  # re-enable. Remember to also re-add `r-check-windows` to `ci-success.needs`
  # and its results array.
  # r-check-windows:
  #   name: R CMD check / Windows
  #   runs-on: windows-latest
  #   timeout-minutes: 90
  #   needs: changes
  #   if: needs.changes.outputs.r == 'true'
  #   # Use Rtools45 MSYS2 bash as the default shell for all run: steps.
  #   # This matches R's own build system (SHELL = sh from Rtools).
  #   # See docs/windows-build-environment.md for details.
  #   defaults:
  #     run:
  #       shell: C:\rtools45\usr\bin\bash.exe -e -o pipefail {0}
  #   env:
  #     GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
  #     R_KEEP_PKG_SOURCE: yes
  #     SCCACHE_GHA_ENABLED: "true"
  #     CARGO_BUILD_TARGET: x86_64-pc-windows-gnu
  #     # Dev profile: R CMD check doesn't benefit from optimized rustc output, and
  #     # MinGW linking is the dominant cost on Windows. Dev profile is ~3-5x faster
  #     # to compile/link than release and keeps codegen-units = 1 (linkme requirement).
  #     CARGO_PROFILE: dev
  #
  #   steps:
  #     - uses: actions/checkout@v7
  #       with:
  #         lfs: true
  #
  #     - name: Setup R
  #       uses: r-lib/actions/setup-r@v2
  #       with:
  #         r-version: release
  #         rtools-version: "45"
  #         use-public-rspm: true
  #
  #     - name: Setup R dependencies
  #       uses: r-lib/actions/setup-r-dependencies@v2
  #       with:
  #         working-directory: rpkg
  #         extra-packages: any::rcmdcheck
  #         needs: check
  #
  #     - name: Install Rust toolchain
  #       uses: dtolnay/rust-toolchain@stable
  #       with:
  #         targets: x86_64-pc-windows-gnu
  #
  #     - name: Setup sccache
  #       id: sccache
  #       uses: mozilla-actions/sccache-action@v0.0.10
  #       continue-on-error: true
  #
  #     - name: Set RUSTC_WRAPPER if sccache available
  #       if: steps.sccache.outcome == 'success'
  #       run: echo "RUSTC_WRAPPER=sccache" >> "$GITHUB_ENV"
  #
  #     - name: Cache Cargo
  #       uses: Swatinem/rust-cache@v2
  #       with:
  #         workspaces: rpkg/src/rust -> target
  #         shared-key: r-windows
  #         cache-on-failure: true
  #
  #     - name: Configure Windows Rust environment
  #       run: |
  #         # Add Rtools to GITHUB_PATH for all subsequent steps
  #         rtools_home="/c/rtools45"
  #         echo "${rtools_home}/x86_64-w64-mingw32.static.posix/bin" >> "$GITHUB_PATH"
  #         echo "${rtools_home}/usr/bin" >> "$GITHUB_PATH"
  #         echo "$(cygpath -u "$(Rscript.exe -e 'cat(R.home())')")/bin/x64" >> "$GITHUB_PATH"
  #         if [ -n "${CARGO_HOME:-}" ]; then
  #           echo "$(cygpath -u "$CARGO_HOME")/bin" >> "$GITHUB_PATH"
  #         fi
  #
  #         # Satisfy rustc's -lgcc_eh / -lgcc_s with the toolchain's real libgcc.
  #         # Rtools45's `.static.posix` gcc ships a unified `libgcc.a` (no separate
  #         # libgcc_eh.a / libgcc_s.a), so we stage symlinks to the real archive.
  #         # Previous versions of this job used empty `ar crs` archives, which
  #         # satisfied the linker but left `__gcc_personality_v0` unresolved at
  #         # runtime — the panic machinery then aborted with
  #         # "fatal runtime error: failed to initiate panic, error 5" whenever
  #         # Rust tried to unwind on Windows. ld.bfd hid this; ld.lld exposes it.
  #         mkdir -p libgcc_mock
  #         real_libgcc="$(x86_64-w64-mingw32.static.posix-gcc.exe -print-libgcc-file-name)"
  #         if [ ! -f "$real_libgcc" ]; then
  #           echo "::error::gcc -print-libgcc-file-name returned non-file: $real_libgcc"
  #           exit 1
  #         fi
  #         cp "$real_libgcc" libgcc_mock/libgcc_eh.a
  #         cp "$real_libgcc" libgcc_mock/libgcc_s.a
  #
  #         # Configure Cargo for Windows GNU target
  #         mkdir -p .cargo
  #         pwd_slash="$(cygpath -m "$(pwd)")"
  #         cat > .cargo/config.toml <<TOML
  #         [target.x86_64-pc-windows-gnu]
  #         linker = "x86_64-w64-mingw32.static.posix-gcc.exe"
  #         # lld is dramatically faster than MinGW's bfd linker. rustup's
  #         # bundled rust-lld (shipped with the toolchain) satisfies -fuse-ld=lld.
  #         rustflags = ["-C", "link-arg=-fuse-ld=lld"]
  #
  #         [env]
  #         LIBRARY_PATH = "${pwd_slash}/libgcc_mock"
  #         TOML
  #
  #     - name: Install just
  #       uses: taiki-e/install-action@just
  #
  #     - name: Install cargo-revendor
  #       run: cargo install --path cargo-revendor
  #
  #     - name: Configure and vendor
  #       run: |
  #         just configure
  #         just vendor
  #
  #     - name: R CMD check
  #       uses: r-lib/actions/check-r-package@v2
  #       with:
  #         working-directory: rpkg
  #         args: 'c("--no-manual", "--as-cran")'
  #         error-on: '"error"'
  #         check-dir: '"check"'
  #         # No NOT_CRAN — configure.ac detects tarball install from inst/vendor.tar.xz
  #       env:
  #         # On Windows, DataFusion's Tokio runtime threads keep the test
  #         # Rterm process alive after tests complete, preventing R CMD check
  #         # from detecting test completion via stdout pipe close. This timeout
  #         # (5 min) kills the test process if it hasn't exited (tests finish
  #         # in ~60s). See reviews/windows-rcmdcheck-hang.md for details.
  #         _R_CHECK_TESTS_ELAPSED_TIMEOUT_: 300
  #
  #     - name: Print failed tests
  #       if: always()
  #       run: |
  #         for f in rpkg/check/*.Rcheck/tests/*.Rout.fail; do
  #           [ -f "$f" ] && echo "=== $f ===" && tail -n100 "$f"
  #         done || true
  #
  #     - name: Print 00check.log and 00install.out
  #       if: always()
  #       run: |
  #         echo "=== 00install.out ==="
  #         cat rpkg/check/*.Rcheck/00install.out 2>/dev/null || echo "00install.out not found"
  #         echo ""
  #         echo "=== 00check.log ==="
  #         cat rpkg/check/*.Rcheck/00check.log 2>/dev/null || echo "00check.log not found"
  #
  #     - name: Upload check results
  #       if: failure()
  #       uses: actions/upload-artifact@v7
  #       with:
  #         name: logs-windows
  #         path: |
  #           rpkg/check
  #           rpkg/config.log
  #         retention-days: 7

  # ===========================================================================
  # R Package: Test suite (Linux only)
  # ===========================================================================
  r-tests:
    name: R tests / Linux
    runs-on: ubuntu-latest
    # Fast source-mode leg: full suite minus the gctorture-heavy files (those
    # run once per PR in r-stress-tests below), plus the heap-check rounds
    # (main/label) and the Valgrind ALTREP pass. Before the stress split this
    # job bundled everything and had outgrown even a 90-min cap on main pushes
    # (suite 34 min + 3 heap-check rounds = ~140 min -> guaranteed timeout).
    timeout-minutes: 60
    needs: changes
    if: needs.changes.outputs.r == 'true'

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      SCCACHE_GHA_ENABLED: "true"
      RUSTC_WRAPPER: "sccache"
      # Stress files run in r-stress-tests; this also keeps the 3x heap-check
      # rounds below within the job budget (they re-run the whole suite).
      MINIEXTENDR_SKIP_STRESS: "1"

    steps:
      - uses: actions/checkout@v7
        with:
          lfs: true

      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: release
          use-public-rspm: true

      - name: Setup R dependencies
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          working-directory: rpkg
          extra-packages: any::testthat
          needs: check

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - uses: mozilla-actions/sccache-action@v0.0.10

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: rpkg/src/rust -> target
          shared-key: r-tests
          cache-on-failure: true

      - name: Install package (source mode)
        # Source-mode smoke test: no vendor tarball, no cargo-revendor needed.
        # configure.ac detects source install from the absence of inst/vendor.tar.xz
        # and lets cargo resolve deps normally (network or monorepo path overrides).
        run: R CMD INSTALL rpkg

      - name: Run tests
        working-directory: rpkg
        shell: bash
        run: Rscript -e "testthat::test_local()"

      - name: Run tests with heap checking (3 rounds, randomized order)
        # Heap checking (glibc MALLOC_CHECK_, 3 randomized rounds) is the
        # malloc-discipline guard. Gate it on PRs behind the `heap-check` label
        # so routine PRs stay fast; still run it unconditionally on
        # push-to-main, the weekly schedule, and manual dispatch so merged code
        # keeps its safety net. The rounds inherit MINIEXTENDR_SKIP_STRESS
        # (~5 min each, not ~35): heap-check hunts malloc/free corruption
        # across the whole suite, while the gctorture files have their own
        # dedicated job — running torture under MALLOC_CHECK_ three extra
        # times is what used to push this job past any timeout.
        if: >-
          github.event_name != 'pull_request' ||
          contains(github.event.pull_request.labels.*.name, 'heap-check')
        working-directory: rpkg
        shell: bash
        env:
          # MALLOC_CHECK_=3: glibc checks heap consistency on every malloc/free
          # and aborts immediately on corruption (not lazily). This catches
          # heap-corrupting UB at the point it happens, not later.
          MALLOC_CHECK_: "3"
          # MALLOC_PERTURB_: fills freed memory with a non-zero pattern,
          # making use-after-free crashes deterministic instead of sporadic.
          MALLOC_PERTURB_: "165"
        run: |
          for i in 1 2 3; do
            echo "=== Heap-checked test run $i/3 ==="
            Rscript -e "testthat::test_local()" || exit 1
          done

      - name: Run ALTREP tests under Valgrind
        if: always()
        working-directory: rpkg
        shell: bash
        run: |
          sudo apt-get install -y valgrind 2>/dev/null || true
          if command -v valgrind &>/dev/null; then
            echo "=== Valgrind memcheck on ALTREP materialization ==="
            R -d "valgrind --tool=memcheck --leak-check=full --track-origins=yes --error-exitcode=1" \
              -e 'library(miniextendr); testthat::test_local(filter = "altrep-materialization")' \
              2>&1 | tee /tmp/valgrind.log || true
            echo "=== Valgrind summary ==="
            grep -A 5 "ERROR SUMMARY\|Invalid read\|Invalid write\|Use of uninitialised" /tmp/valgrind.log || echo "No errors found"
          else
            echo "Valgrind not available, skipping"
          fi

  # ===========================================================================
  # R Package: GC-stress tests (gctorture) — sharded
  # ===========================================================================
  # The single place the gctorture-heavy test files run per PR (every other
  # suite-running job sets MINIEXTENDR_SKIP_STRESS=1 — they were all running
  # the same ~31 min of torture, five times per push). The dominant cost is
  # the dynamic no-arg fixture sweep in test-gc-stress-fixtures.R; it splits
  # across the shards by fixture index (MINIEXTENDR_STRESS_SHARD=k/n, see
  # helper-gc-stress.R), and the other stress files are distributed via the
  # testthat filter so the shards stay balanced. Per-PR coverage is unchanged:
  # same fixtures, same iteration counts, run once instead of five times. The
  # nightly gctorture-nightly.yml step=100 full-suite sweep remains the deep net.
  r-stress-tests:
    name: R GC-stress tests / shard ${{ matrix.shard }}
    runs-on: ubuntu-latest
    timeout-minutes: 60
    needs: changes
    if: needs.changes.outputs.r == 'true'
    strategy:
      fail-fast: false
      # Three shards, balanced from measured timings (run 29073963629: the
      # 2-shard layout ran 20.0 / 20.9 min of tests; the sweep is ~24 min of
      # that total, the explicit fixture-file blocks ~3 min, the three whole
      # files ~4 / ~3 / ~2.5 min). Each shard = one sweep third (~8 min) +
      # the explicit blocks (~3 min, run per-shard — the sweep file is in
      # every filter) + one whole stress file, so ~14-15 min of tests each.
      matrix:
        include:
          # Shard 1: sweep third 1 + ExternalPtr roots + cold factor caches.
          - shard: "1/3"
            filter: "gc-stress-fixtures|externalptr-self-root|factor-construction-gc"
          # Shard 2: sweep third 2 + the iter-to-dataframe file.
          - shard: "2/3"
            filter: "gc-stress-fixtures|iter-to-dataframe"
          # Shard 3: sweep third 3 + the dataframe-deserialize file.
          - shard: "3/3"
            filter: "gc-stress-fixtures|dataframe-deserialize"

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      SCCACHE_GHA_ENABLED: "true"
      RUSTC_WRAPPER: "sccache"
      MINIEXTENDR_STRESS_SHARD: ${{ matrix.shard }}

    steps:
      - uses: actions/checkout@v7
        with:
          lfs: true

      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: release
          use-public-rspm: true

      - name: Setup R dependencies
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          working-directory: rpkg
          extra-packages: any::testthat
          needs: check

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - uses: mozilla-actions/sccache-action@v0.0.10

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: rpkg/src/rust -> target
          # Same source-mode build as r-tests — share its cache rather than
          # keeping per-shard copies of an identical target dir.
          shared-key: r-tests
          cache-on-failure: true

      - name: Install package (source mode)
        # Same source-mode install as r-tests: no vendor tarball, configure.ac
        # detects source install and resolves the workspace via path overrides.
        run: R CMD INSTALL rpkg

      - name: Run GC-stress tests
        working-directory: rpkg
        shell: bash
        run: Rscript -e 'testthat::test_local(filter = "${{ matrix.filter }}")'

  # ===========================================================================
  # Feature runtime legs (scheduled only)
  # ===========================================================================
  # rpkg's tools/detect-features.R denylists the codegen selectors
  # (worker-default, strict-default, coerce-default, fast-default, r6-default,
  # s7-default), nonapi, indicatif, and growth-debug, so no PR-gating job ever
  # builds — let alone runs — the R wrappers those features generate
  # (audit A5/A10). Each leg rebuilds rpkg with one bundle of denylisted
  # features on top of the auto-detected base set and runs testthat against it.
  #
  # The extras, worker-default, r6-default, and s7-default legs run the full
  # suite. extras/worker-default features are additive/transparent; the class
  # legs became full-suite-safe in #1113, which made every #[miniextendr] impl
  # block in rpkg explicitly `env` — the class flip now only reaches the one
  # deliberately bare probe (FdefaultProbe in feature_default_fixtures.rs) plus
  # the divergence assertions in tests/testthat/test-feature-defaults.R.
  # The strict-default, coerce-default, and fast-default legs stay filtered to
  # the feature-default fixture tests: they flip conversion semantics /
  # preconditions for every parameter suite-wide, so the pre-existing suite
  # fails by design under them.
  # The fast-default leg additionally runs the two fast_default unit tests in
  # miniextendr-macros/src/tests.rs, which no other job compiles+runs.
  #
  # Weekly-only + workflow_dispatch: seven extra full rpkg builds are too heavy
  # for a per-PR gate. Intentionally NOT in ci-success.needs; failures surface
  # via the job summary (same pattern as gctorture-nightly.yml).
  feature-legs:
    name: Feature runtime leg / ${{ matrix.leg }}
    runs-on: ubuntu-latest
    timeout-minutes: 90
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    strategy:
      fail-fast: false
      matrix:
        include:
          - leg: extras
            features: nonapi,indicatif,growth-debug
            filter: ""
          - leg: worker-default
            features: worker-default
            filter: ""
          # The class legs run the full suite (#1113): rpkg's impl blocks are
          # explicitly `env`, so the class flip only reaches the deliberate
          # probe. strict/coerce cannot ride on them — they flip conversion
          # semantics suite-wide — so they get their own fixture-filtered
          # rows. They must also stay separate from EACH OTHER: under
          # coerce-default, lossy-int params convert via R's integer native
          # type, which masks the strict/no_strict divergence the fixtures
          # assert (#1112).
          - leg: r6-default
            features: r6-default
            filter: ""
          - leg: s7-default
            features: s7-default
            filter: ""
          - leg: strict-default
            features: strict-default
            filter: feature-defaults
          - leg: coerce-default
            features: coerce-default
            filter: feature-defaults
          - leg: fast-default
            features: fast-default
            filter: feature-defaults

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      SCCACHE_GHA_ENABLED: "true"
      RUSTC_WRAPPER: "sccache"
      # gctorture coverage lives in the sharded r-stress-tests job + nightly.
      MINIEXTENDR_SKIP_STRESS: "1"

    steps:
      - uses: actions/checkout@v7
        with:
          lfs: true

      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: release
          use-public-rspm: true

      - name: Setup R dependencies
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          working-directory: rpkg
          extra-packages: any::testthat
          needs: check

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - uses: mozilla-actions/sccache-action@v0.0.10

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: rpkg/src/rust -> target
          shared-key: feature-leg-${{ matrix.leg }}
          cache-on-failure: true

      # The fast-default leg is also the only place the two fast_default
      # unit tests in miniextendr-macros/src/tests.rs run: clippy_all compiles
      # them (full-codegen includes fast-default) but never executes them.
      - name: Run fast_default unit tests (miniextendr-macros)
        if: matrix.leg == 'fast-default'
        run: cargo test -p miniextendr-macros --features fast-default fast_default

      # configure.ac skips feature auto-detection when CARGO_FEATURES is set,
      # so the base set is composed here: what detect-features.R would have
      # picked on this runner, plus the leg's denylisted features. Rust must
      # already be on PATH (the arrow/datafusion rules probe rustc --version).
      - name: Compose CARGO_FEATURES (detected base + leg features)
        run: |
          base="$(Rscript rpkg/tools/detect-features.R)"
          if [ -z "$base" ]; then
            echo "detect-features.R returned an empty base feature set" >&2
            exit 1
          fi
          echo "CARGO_FEATURES=${base},${{ matrix.features }}" >> "$GITHUB_ENV"
          echo "CARGO_FEATURES = ${base},${{ matrix.features }}"

      - name: Install package (source mode)
        # Same source-mode install as r-tests: no vendor tarball, configure
        # picks up CARGO_FEATURES from the environment.
        run: R CMD INSTALL rpkg

      - name: Run tests
        working-directory: rpkg
        shell: bash
        run: |
          if [ -n "${{ matrix.filter }}" ]; then
            Rscript -e 'testthat::test_local(filter = "${{ matrix.filter }}")'
          else
            Rscript -e 'testthat::test_local()'
          fi

      - name: Surface failure in job summary
        if: failure()
        run: |
          {
            echo "## Feature runtime leg '${{ matrix.leg }}' FAILED"
            echo ""
            echo "CARGO_FEATURES: \`$CARGO_FEATURES\`"
            echo ""
            echo "This scheduled leg is the only runtime coverage for its"
            echo "denylisted features. Reproduce locally with:"
            echo ""
            echo '```'
            echo "CARGO_FEATURES=\"$CARGO_FEATURES\" R CMD INSTALL rpkg"
            echo "cd rpkg && Rscript -e 'testthat::test_local(filter = \"${{ matrix.filter }}\")'  # empty filter = full suite"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

  # ===========================================================================
  # Cross-package trait ABI tests
  # ===========================================================================
  cross-package-tests:
    name: Cross-Package Trait ABI
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: changes
    if: needs.changes.outputs.r == 'true'
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      SCCACHE_GHA_ENABLED: "true"
      RUSTC_WRAPPER: "sccache"

    steps:
      - uses: actions/checkout@v7
        with:
          lfs: true

      - name: Install just
        uses: taiki-e/install-action@just

      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: release
          use-public-rspm: true

      - name: Setup R dependencies
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          working-directory: rpkg
          extra-packages: any::testthat, any::devtools
          needs: check

      - name: Install autoconf
        # ubuntu-latest images ship autoconf; the apt path is only a fallback
        # for image changes (saves ~30-60 s of apt-get update per job).
        run: |
          command -v autoconf >/dev/null || { sudo apt-get update && sudo apt-get install -y autoconf; }

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - uses: mozilla-actions/sccache-action@v0.0.10

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            tests/cross-package/producer.pkg/src/rust -> target
            tests/cross-package/consumer.pkg/src/rust -> target
          shared-key: cross-package
          cache-on-failure: true

      - name: Configure Linux
        run: |
          echo "LD_LIBRARY_PATH=$(R RHOME)/lib" >> "$GITHUB_ENV"

      - name: Build and test cross-package packages
        working-directory: tests/cross-package
        run: |
          just build-all
          just test-all

      # Wrappers sync gate (#1218): unlike rpkg's (gitignored) wrappers.R,
      # producer.pkg/consumer.pkg wrappers.R are TRACKED generated files, and
      # `just build-all` above regenerates them in-tree. If macro codegen
      # changed without the committed snapshots being regenerated, the diff is
      # non-empty here — the cross-package analogue of `wrappers-sync-check`.
      - name: Check committed wrappers.R in sync
        run: |
          git diff --exit-code -- \
            tests/cross-package/producer.pkg/R/producer.pkg-wrappers.R \
            tests/cross-package/consumer.pkg/R/consumer.pkg-wrappers.R \
          || { echo "::error::cross-package wrappers.R drifted from codegen — run 'just cross-install' and commit the regenerated files (#1218)"; exit 1; }

  # ===========================================================================
  # minirextendr (scaffolding helper package)
  # ===========================================================================
  minirextendr:
    name: minirextendr
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: changes
    if: needs.changes.outputs.r == 'true'
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes

    steps:
      - uses: actions/checkout@v7
        with:
          lfs: true

      - name: Install just
        uses: taiki-e/install-action@just

      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: release
          use-public-rspm: true

      - name: Setup R dependencies
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          working-directory: minirextendr
          extra-packages: any::rcmdcheck, any::testthat, any::devtools
          needs: check

      - name: Install autoconf
        # ubuntu-latest images ship autoconf; the apt path is only a fallback
        # for image changes (saves ~30-60 s of apt-get update per job).
        run: |
          command -v autoconf >/dev/null || { sudo apt-get update && sudo apt-get install -y autoconf; }

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Run tests
        working-directory: minirextendr
        env:
          MINIEXTENDR_LOCAL_PATH: ${{ github.workspace }}
        run: Rscript -e "testthat::set_max_fails(Inf); testthat::test_local()"

      - name: R CMD check
        uses: r-lib/actions/check-r-package@v2
        with:
          working-directory: minirextendr
          args: 'c("--no-manual", "--as-cran")'
          error-on: '"warning"'
          check-dir: '"check"'

      - name: Upload check results
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: logs-minirextendr
          path: minirextendr/check
          retention-days: 7

  # ===========================================================================
  # CRAN-like check (stricter, simulates CRAN environment)
  # ===========================================================================
  cran-check:
    name: CRAN-like check
    runs-on: ubuntu-latest
    timeout-minutes: 60
    needs: changes
    if: needs.changes.outputs.r == 'true'
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes
      # Tarball install mode is detected by configure.ac from the presence of
      # inst/vendor.tar.xz (produced by `just vendor` below). No NOT_CRAN needed.
      SCCACHE_GHA_ENABLED: "true"
      RUSTC_WRAPPER: "sccache"
      # Skip the gctorture files here for CRAN realism as much as speed: their
      # helper also calls skip_on_cran(), because a half-hour test suite would
      # be rejected by real CRAN. The explicit env var is still needed —
      # r-lib's setup actions export NOT_CRAN=true into every job, so
      # skip_on_cran() alone never fires in CI. See helper-gc-stress.R.
      MINIEXTENDR_SKIP_STRESS: "1"

    steps:
      - uses: actions/checkout@v7
        with:
          lfs: true

      - name: Install just
        uses: taiki-e/install-action@just

      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: release
          use-public-rspm: true

      - name: Setup R dependencies
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          working-directory: rpkg
          extra-packages: any::rcmdcheck
          needs: check

      - name: Install autoconf
        # ubuntu-latest images ship autoconf; the apt path is only a fallback
        # for image changes (saves ~30-60 s of apt-get update per job).
        run: |
          command -v autoconf >/dev/null || { sudo apt-get update && sudo apt-get install -y autoconf; }

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - uses: mozilla-actions/sccache-action@v0.0.10

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: rpkg/src/rust -> target
          shared-key: cran-check
          cache-on-failure: true

      - name: Configure Linux
        run: |
          echo "LD_LIBRARY_PATH=$(R RHOME)/lib" >> "$GITHUB_ENV"

      # NOTE: the vendor tarball is intentionally NOT cached. It is always
      # regenerated by `just vendor` (--force) in the step below, so caching it
      # bought nothing — and a restored tarball flipped `just configure` into
      # tarball mode, which made cargo-revendor drop in-PR workspace-crate edits
      # and re-fetch the framework from git@main (latch leak, #441 / #865).
      - name: Set up cargo-revendor and vendor
        uses: ./.github/actions/setup-vendor

      - name: Build source tarball
        run: |
          # Remove vendor/ so tarball only contains inst/vendor.tar.xz
          rm -rf rpkg/vendor
          R CMD build rpkg
          echo "TARBALL=$(ls -1 miniextendr_*.tar.gz)" >> $GITHUB_ENV

      - name: R CMD check (CRAN-like, from tarball)
        run: |
          # Check the tarball without NOT_CRAN — configure.ac detects tarball
          # install mode from the presence of inst/vendor.tar.xz (shipped inside
          # the built tarball by `just vendor` above).
          R CMD check --as-cran --no-manual ${{ env.TARBALL }}

      - name: Copy check results
        if: always()
        run: |
          mkdir -p rpkg/check
          mv miniextendr.Rcheck rpkg/check/ 2>/dev/null || true

      - name: Print failed tests
        if: always()
        run: |
          for f in rpkg/check/*.Rcheck/tests/*.Rout.fail; do
            [ -f "$f" ] && echo "=== $f ===" && tail -n100 "$f"
          done || true

      - name: Print 00check.log and 00install.out
        if: always()
        run: |
          echo "=== 00install.out ==="
          cat rpkg/check/*.Rcheck/00install.out 2>/dev/null || echo "00install.out not found"
          echo ""
          echo "=== 00check.log ==="
          cat rpkg/check/*.Rcheck/00check.log 2>/dev/null || echo "00check.log not found"

      - name: Upload check results
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: logs-cran-check
          path: |
            rpkg/check
            rpkg/config.log
          retention-days: 7

  # ===========================================================================
  # Bootstrap vendor regression test (#441 latch)
  # ===========================================================================
  bootstrap-vendor-test:
    name: Bootstrap Vendor Test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: changes
    if: needs.changes.outputs.r == 'true'
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      SCCACHE_GHA_ENABLED: "true"
      RUSTC_WRAPPER: "sccache"

    steps:
      - uses: actions/checkout@v7
        with:
          lfs: true

      - name: Install just
        uses: taiki-e/install-action@just

      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: release
          use-public-rspm: true

      - name: Install rv (for .Rprofile activate.R)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          RV_VERSION=$(gh api repos/A2-ai/rv/releases/latest --jq .tag_name)
          : "${RV_VERSION:?empty rv version}"
          curl --proto '=https' --tlsv1.2 --retry 5 --retry-connrefused --location \
               --silent --show-error --fail \
               "https://github.com/A2-ai/rv/releases/download/${RV_VERSION}/rv-${RV_VERSION}-x86_64-unknown-linux-gnu.tar.gz" \
               -o /tmp/rv.tar.gz
          # Tarball ships a flat `rv` binary at the top level — no subdir.
          tar -xzf /tmp/rv.tar.gz -C /tmp
          sudo install -m 0755 /tmp/rv /usr/local/bin/rv
          rv --version

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - uses: mozilla-actions/sccache-action@v0.0.10

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          # bootstrap.R calls cargo-revendor which calls `cargo metadata` on the
          # rpkg workspace — cache that workspace's target dir.
          workspaces: rpkg/src/rust -> target
          shared-key: bootstrap-vendor-test
          cache-on-failure: true

      - name: Set up cargo-revendor (install only)
        uses: ./.github/actions/setup-vendor
        with:
          configure-and-vendor: 'false'

      - name: Run bootstrap-vendor regression test
        run: just test-bootstrap-vendor

  # ===========================================================================
  # Standalone round-trip e2e (on-demand: nightly cron + label + dispatch)
  # ===========================================================================
  # Exercises the full standalone (non-monorepo) flow that the per-PR suite
  # cannot afford: create_miniextendr_package() -> miniextendr_build() (auto-
  # vendor flips to tarball mode mid-install) -> wrapper-gen -> install ->
  # library() exposes the functions. This is the #757/#775 regression target:
  # any change that makes a build entry point bypass MINIEXTENDR_FORCE_WRAPPER_GEN
  # (or alters the tarball-mode skip) re-introduces the empty-namespace bug.
  #
  # The assertions live in the gated testthat round-trip at
  # minirextendr/tests/testthat/test-templates.R:521 — CI runs the suite, it does
  # NOT reimplement the checks in shell (per #805). MINIEXTENDR_RUN_E2E=1 lifts
  # skip_e2e()'s skip_on_ci() so the otherwise-skipped e2e tests actually run.
  #
  # On-demand only — NOT in ci-success.needs, so it never gates a PR and adds no
  # minutes to normal runs. Triggers: weekly cron (Sat 06:00 UTC), the `run-e2e`
  # PR label, or `gh workflow run ci.yml --ref <branch>`. Heavy: cold-compiles
  # Rust + autoconf + network-fetches/vendors miniextendr `main`.
  r-roundtrip-e2e:
    name: Standalone round-trip e2e
    runs-on: ubuntu-latest
    timeout-minutes: 45
    if: >-
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'schedule' ||
      contains(github.event.pull_request.labels.*.name, 'run-e2e')
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes
      SCCACHE_GHA_ENABLED: "true"
      RUSTC_WRAPPER: "sccache"

    steps:
      - uses: actions/checkout@v7
        with:
          lfs: true

      - name: Install just
        uses: taiki-e/install-action@just

      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: release
          use-public-rspm: true

      - name: Setup R dependencies
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          working-directory: minirextendr
          extra-packages: any::testthat, any::devtools, any::withr, any::roxygen2
          needs: check

      - name: Install autoconf
        # ubuntu-latest images ship autoconf; the apt path is only a fallback
        # for image changes (saves ~30-60 s of apt-get update per job).
        run: |
          command -v autoconf >/dev/null || { sudo apt-get update && sudo apt-get install -y autoconf; }

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - uses: mozilla-actions/sccache-action@v0.0.10

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: rpkg/src/rust -> target
          shared-key: roundtrip-e2e
          cache-on-failure: true

      # cargo-revendor on PATH so the standalone build's auto-vendor (tarball
      # mode) can seal inst/vendor.tar.xz mid-install.
      - name: Set up cargo-revendor (install only)
        uses: ./.github/actions/setup-vendor
        with:
          configure-and-vendor: 'false'

      - name: Run standalone round-trip e2e
        run: just minirextendr-roundtrip

  # ===========================================================================
  # Summary job (for branch protection)
  # ===========================================================================
  ci-success:
    name: CI Success
    needs:
      - generated-files-check
      - version-check
      - sync-checks
      - docs-link-check
      - rust-lint
      - rust-tests
      - changes
      - r-check-linux
      # - r-check-macos   # non-gating: runs only on workflow_dispatch + weekly cron, see #95
      # - r-check-windows # DISABLED — re-enable when Windows job is uncommented above
      - r-tests
      - r-stress-tests
      - cross-package-tests
      - minirextendr
      - cran-check
      - bootstrap-vendor-test
      # - r-roundtrip-e2e # non-gating: on-demand only (cron / run-e2e label / dispatch), see #775
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Check all jobs passed
        run: |
          results=(
            "${{ needs.generated-files-check.result }}"
            "${{ needs.version-check.result }}"
            "${{ needs.sync-checks.result }}"
            "${{ needs.docs-link-check.result }}"
            "${{ needs.rust-lint.result }}"
            "${{ needs.rust-tests.result }}"
            "${{ needs.changes.result }}"
            "${{ needs.r-check-linux.result }}"
            # "${{ needs.r-check-macos.result }}"   # non-gating, see #95
            # "${{ needs.r-check-windows.result }}" # DISABLED
            "${{ needs.r-tests.result }}"
            "${{ needs.r-stress-tests.result }}"
            "${{ needs.cross-package-tests.result }}"
            "${{ needs.minirextendr.result }}"
            "${{ needs.cran-check.result }}"
            "${{ needs.bootstrap-vendor-test.result }}"
          )
          for result in "${results[@]}"; do
            if [[ "$result" != "success" && "$result" != "skipped" ]]; then
              echo "One or more jobs failed: $result"
              exit 1
            fi
          done
          echo "All CI jobs passed!"
